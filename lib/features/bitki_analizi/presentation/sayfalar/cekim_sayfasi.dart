import 'package:flutter/material.dart';

class CekimSayfasi extends StatelessWidget {
  const CekimSayfasi({super.key});
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Kamera entegrasyonu eklenecek')),
    );
  }
}
